# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Wbt' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Wbt

  target 'WbtTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WbtUITests' do
    # Pods for testing
  end

  pod 'Motion'
  pod 'lottie-ios','~> 2.5.2'
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftMessages'
  pod 'Kingfisher', '~> 5.0'
  pod 'Shimmer'
end
